{
  "version": "0.2.0",
  "configurations": [
    {
      "type": "chrome",
      "request": "launch",
      "name": "localhost (Chrome)",
      "url": "http://localhost:44325",
      "webRoot": "${workspaceFolder}"
    },
    {
      "type": "msedge",
      "request": "launch",
      "name": "localhost (Edge)",
      "url": "http://localhost:44325",
      "webRoot": "${workspaceFolder}"
    }
  ]
}